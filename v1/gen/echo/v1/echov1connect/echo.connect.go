// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: echo/v1/echo.proto

package echov1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/syedjafri06193/microservices-api-framework/gen/echo/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// EchoServiceName is the fully-qualified name of the EchoService service.
	EchoServiceName = "echo.v1.EchoService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EchoServiceEchoProcedure is the fully-qualified name of the EchoService's Echo RPC.
	EchoServiceEchoProcedure = "/echo.v1.EchoService/Echo"
	// EchoServiceSetValueProcedure is the fully-qualified name of the EchoService's SetValue RPC.
	EchoServiceSetValueProcedure = "/echo.v1.EchoService/SetValue"
	// EchoServiceChargeProcedure is the fully-qualified name of the EchoService's Charge RPC.
	EchoServiceChargeProcedure = "/echo.v1.EchoService/Charge"
	// EchoServiceFailProcedure is the fully-qualified name of the EchoService's Fail RPC.
	EchoServiceFailProcedure = "/echo.v1.EchoService/Fail"
	// EchoServicePanicProcedure is the fully-qualified name of the EchoService's Panic RPC.
	EchoServicePanicProcedure = "/echo.v1.EchoService/Panic"
	// EchoServiceSlowProcedure is the fully-qualified name of the EchoService's Slow RPC.
	EchoServiceSlowProcedure = "/echo.v1.EchoService/Slow"
)

// EchoServiceClient is a client for the echo.v1.EchoService service.
type EchoServiceClient interface {
	// Safe to retry: no side effects at all.
	Echo(context.Context, *connect.Request[v1.EchoRequest]) (*connect.Response[v1.EchoResponse], error)
	// Safe to retry: repeating it produces the same state.
	SetValue(context.Context, *connect.Request[v1.SetValueRequest]) (*connect.Response[v1.SetValueResponse], error)
	// NOT annotated, and deliberately named to make the stakes obvious. The
	// framework will never auto-retry this, because retrying it would charge
	// the card twice.
	Charge(context.Context, *connect.Request[v1.ChargeRequest]) (*connect.Response[v1.ChargeResponse], error)
	// Always fails, for exercising the breaker and the retry budget.
	Fail(context.Context, *connect.Request[v1.FailRequest]) (*connect.Response[v1.FailResponse], error)
	// Panics, for proving the recovery interceptor is innermost and that the
	// panic is observed by tracing, logging and metrics on the way out.
	Panic(context.Context, *connect.Request[v1.PanicRequest]) (*connect.Response[v1.PanicResponse], error)
	// Sleeps, for exercising deadlines and load shedding.
	Slow(context.Context, *connect.Request[v1.SlowRequest]) (*connect.Response[v1.SlowResponse], error)
}

// NewEchoServiceClient constructs a client for the echo.v1.EchoService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEchoServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EchoServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	echoServiceMethods := v1.File_echo_v1_echo_proto.Services().ByName("EchoService").Methods()
	return &echoServiceClient{
		echo: connect.NewClient[v1.EchoRequest, v1.EchoResponse](
			httpClient,
			baseURL+EchoServiceEchoProcedure,
			connect.WithSchema(echoServiceMethods.ByName("Echo")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		setValue: connect.NewClient[v1.SetValueRequest, v1.SetValueResponse](
			httpClient,
			baseURL+EchoServiceSetValueProcedure,
			connect.WithSchema(echoServiceMethods.ByName("SetValue")),
			connect.WithIdempotency(connect.IdempotencyIdempotent),
			connect.WithClientOptions(opts...),
		),
		charge: connect.NewClient[v1.ChargeRequest, v1.ChargeResponse](
			httpClient,
			baseURL+EchoServiceChargeProcedure,
			connect.WithSchema(echoServiceMethods.ByName("Charge")),
			connect.WithClientOptions(opts...),
		),
		fail: connect.NewClient[v1.FailRequest, v1.FailResponse](
			httpClient,
			baseURL+EchoServiceFailProcedure,
			connect.WithSchema(echoServiceMethods.ByName("Fail")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		panic: connect.NewClient[v1.PanicRequest, v1.PanicResponse](
			httpClient,
			baseURL+EchoServicePanicProcedure,
			connect.WithSchema(echoServiceMethods.ByName("Panic")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		slow: connect.NewClient[v1.SlowRequest, v1.SlowResponse](
			httpClient,
			baseURL+EchoServiceSlowProcedure,
			connect.WithSchema(echoServiceMethods.ByName("Slow")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
	}
}

// echoServiceClient implements EchoServiceClient.
type echoServiceClient struct {
	echo     *connect.Client[v1.EchoRequest, v1.EchoResponse]
	setValue *connect.Client[v1.SetValueRequest, v1.SetValueResponse]
	charge   *connect.Client[v1.ChargeRequest, v1.ChargeResponse]
	fail     *connect.Client[v1.FailRequest, v1.FailResponse]
	panic    *connect.Client[v1.PanicRequest, v1.PanicResponse]
	slow     *connect.Client[v1.SlowRequest, v1.SlowResponse]
}

// Echo calls echo.v1.EchoService.Echo.
func (c *echoServiceClient) Echo(ctx context.Context, req *connect.Request[v1.EchoRequest]) (*connect.Response[v1.EchoResponse], error) {
	return c.echo.CallUnary(ctx, req)
}

// SetValue calls echo.v1.EchoService.SetValue.
func (c *echoServiceClient) SetValue(ctx context.Context, req *connect.Request[v1.SetValueRequest]) (*connect.Response[v1.SetValueResponse], error) {
	return c.setValue.CallUnary(ctx, req)
}

// Charge calls echo.v1.EchoService.Charge.
func (c *echoServiceClient) Charge(ctx context.Context, req *connect.Request[v1.ChargeRequest]) (*connect.Response[v1.ChargeResponse], error) {
	return c.charge.CallUnary(ctx, req)
}

// Fail calls echo.v1.EchoService.Fail.
func (c *echoServiceClient) Fail(ctx context.Context, req *connect.Request[v1.FailRequest]) (*connect.Response[v1.FailResponse], error) {
	return c.fail.CallUnary(ctx, req)
}

// Panic calls echo.v1.EchoService.Panic.
func (c *echoServiceClient) Panic(ctx context.Context, req *connect.Request[v1.PanicRequest]) (*connect.Response[v1.PanicResponse], error) {
	return c.panic.CallUnary(ctx, req)
}

// Slow calls echo.v1.EchoService.Slow.
func (c *echoServiceClient) Slow(ctx context.Context, req *connect.Request[v1.SlowRequest]) (*connect.Response[v1.SlowResponse], error) {
	return c.slow.CallUnary(ctx, req)
}

// EchoServiceHandler is an implementation of the echo.v1.EchoService service.
type EchoServiceHandler interface {
	// Safe to retry: no side effects at all.
	Echo(context.Context, *connect.Request[v1.EchoRequest]) (*connect.Response[v1.EchoResponse], error)
	// Safe to retry: repeating it produces the same state.
	SetValue(context.Context, *connect.Request[v1.SetValueRequest]) (*connect.Response[v1.SetValueResponse], error)
	// NOT annotated, and deliberately named to make the stakes obvious. The
	// framework will never auto-retry this, because retrying it would charge
	// the card twice.
	Charge(context.Context, *connect.Request[v1.ChargeRequest]) (*connect.Response[v1.ChargeResponse], error)
	// Always fails, for exercising the breaker and the retry budget.
	Fail(context.Context, *connect.Request[v1.FailRequest]) (*connect.Response[v1.FailResponse], error)
	// Panics, for proving the recovery interceptor is innermost and that the
	// panic is observed by tracing, logging and metrics on the way out.
	Panic(context.Context, *connect.Request[v1.PanicRequest]) (*connect.Response[v1.PanicResponse], error)
	// Sleeps, for exercising deadlines and load shedding.
	Slow(context.Context, *connect.Request[v1.SlowRequest]) (*connect.Response[v1.SlowResponse], error)
}

// NewEchoServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEchoServiceHandler(svc EchoServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	echoServiceMethods := v1.File_echo_v1_echo_proto.Services().ByName("EchoService").Methods()
	echoServiceEchoHandler := connect.NewUnaryHandler(
		EchoServiceEchoProcedure,
		svc.Echo,
		connect.WithSchema(echoServiceMethods.ByName("Echo")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	echoServiceSetValueHandler := connect.NewUnaryHandler(
		EchoServiceSetValueProcedure,
		svc.SetValue,
		connect.WithSchema(echoServiceMethods.ByName("SetValue")),
		connect.WithIdempotency(connect.IdempotencyIdempotent),
		connect.WithHandlerOptions(opts...),
	)
	echoServiceChargeHandler := connect.NewUnaryHandler(
		EchoServiceChargeProcedure,
		svc.Charge,
		connect.WithSchema(echoServiceMethods.ByName("Charge")),
		connect.WithHandlerOptions(opts...),
	)
	echoServiceFailHandler := connect.NewUnaryHandler(
		EchoServiceFailProcedure,
		svc.Fail,
		connect.WithSchema(echoServiceMethods.ByName("Fail")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	echoServicePanicHandler := connect.NewUnaryHandler(
		EchoServicePanicProcedure,
		svc.Panic,
		connect.WithSchema(echoServiceMethods.ByName("Panic")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	echoServiceSlowHandler := connect.NewUnaryHandler(
		EchoServiceSlowProcedure,
		svc.Slow,
		connect.WithSchema(echoServiceMethods.ByName("Slow")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	return "/echo.v1.EchoService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EchoServiceEchoProcedure:
			echoServiceEchoHandler.ServeHTTP(w, r)
		case EchoServiceSetValueProcedure:
			echoServiceSetValueHandler.ServeHTTP(w, r)
		case EchoServiceChargeProcedure:
			echoServiceChargeHandler.ServeHTTP(w, r)
		case EchoServiceFailProcedure:
			echoServiceFailHandler.ServeHTTP(w, r)
		case EchoServicePanicProcedure:
			echoServicePanicHandler.ServeHTTP(w, r)
		case EchoServiceSlowProcedure:
			echoServiceSlowHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEchoServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEchoServiceHandler struct{}

func (UnimplementedEchoServiceHandler) Echo(context.Context, *connect.Request[v1.EchoRequest]) (*connect.Response[v1.EchoResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("echo.v1.EchoService.Echo is not implemented"))
}

func (UnimplementedEchoServiceHandler) SetValue(context.Context, *connect.Request[v1.SetValueRequest]) (*connect.Response[v1.SetValueResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("echo.v1.EchoService.SetValue is not implemented"))
}

func (UnimplementedEchoServiceHandler) Charge(context.Context, *connect.Request[v1.ChargeRequest]) (*connect.Response[v1.ChargeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("echo.v1.EchoService.Charge is not implemented"))
}

func (UnimplementedEchoServiceHandler) Fail(context.Context, *connect.Request[v1.FailRequest]) (*connect.Response[v1.FailResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("echo.v1.EchoService.Fail is not implemented"))
}

func (UnimplementedEchoServiceHandler) Panic(context.Context, *connect.Request[v1.PanicRequest]) (*connect.Response[v1.PanicResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("echo.v1.EchoService.Panic is not implemented"))
}

func (UnimplementedEchoServiceHandler) Slow(context.Context, *connect.Request[v1.SlowRequest]) (*connect.Response[v1.SlowResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("echo.v1.EchoService.Slow is not implemented"))
}
